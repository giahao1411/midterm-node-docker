<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/course">Blog Khóa Học</a>
        <button
            class="navbar-toggler"
            type="button"
            data-toggle="collapse"
            data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <!-- Search Form -->
                <form class="form-inline my-2 my-lg-0" id="searchForm" action="/search" method="get">
                    <input
                        class="form-control form-control-sm mr-sm-2 search-input"
                        type="search"
                        placeholder="Tìm kiếm"
                        aria-label="Search"
                        name="query"
                        id="searchInput"
                    />
                    <button
                        class="btn btn-outline-success btn-sm my-2 my-sm-0 search-btn"
                        type="submit"
                    >
                        <i class="bi bi-search"></i>
                    </button>
                </form>

                <!-- User Dropdown -->
                <li class="nav-item dropdown">
                    <a
                        class="nav-link dropdown-toggle"
                        href="#"
                        id="navbarDropdown"
                        role="button"
                        data-toggle="dropdown"
                        aria-haspopup="true"
                        aria-expanded="false"
                    >
                        <img
                            src="/images/logo-tdtu.jpg"
                            alt=""
                            class="user-avater"
                        />
                        <%= userName %>
                    </a>

                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <% if (userRole === 'admin') { %>
                        <a class="dropdown-item" href="/course/create"
                            >Đăng khóa học</a
                        >
                        <a class="dropdown-item" href="/me">Quản lý khóa học</a>
                        <div class="dropdown-divider"></div>
                        <% } %>
                        <a class="dropdown-item" href="/me/purchased"
                            >Khóa học của tôi</a
                        >
                        <a class="dropdown-item" href="/me/cart">Giỏ hàng</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/login/logout"
                            >Đăng xuất</a
                        >
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
